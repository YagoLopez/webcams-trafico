<artifact>
<id>tasks</id>
<change>filter-webcams-dropdown</change>
<type>tasks</type>

<tasks>
- [x] Create `RoadSelector` modal component (or implement inline in `WebcamsListScreen`).
- [x] Replace existing province chips UI with `RoadSelector` trigger (Dropdown).
- [x] Update state to `selectedRoad` in `WebcamsListScreen`.
- [x] Implement `uniqueRoads` extraction logic.
- [x] Wire up filtering logic based on `selectedRoad`.
- [ ] Verify functionality (Open modal -> Select Road -> Filter Works).
</tasks>

</artifact>
