# center-filters-modal Unit Tests Plan

## Unit Tests Checklist

- [ ] `components/__tests__/filters-modal.test.tsx`: Verify that the component renders without crashing and the Modal component receives the correct `animationType` prop ("fade").
- [ ] `components/__tests__/filters-modal.test.tsx`: Verify that the CSS classes applied to the container views reflect the new centered design (`justify-center`, `items-center`, `rounded-3xl`, `max-w-md`).
- [ ] `components/ui/__tests__/select-box.test.tsx`: Verify that the component renders without crashing and the internal Modal component receives the correct `animationType` prop ("fade").
- [ ] `components/ui/__tests__/select-box.test.tsx`: Verify that the CSS classes applied to the container views of the internal modal reflect the new design (`justify-start`, `pt-20`, `items-center`, `rounded-3xl`, `max-h-[80%]`, `max-w-md`).
