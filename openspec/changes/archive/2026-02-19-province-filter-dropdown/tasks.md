<artifact>
<id>tasks</id>
<change>province-filter-dropdown</change>
<type>tasks</type>

<tasks>
- [x] Create `components/common` directory if not exists.
- [x] Implement `SelectBox.tsx` in `components/ui/SelectBox.tsx`.
- [x] Refactor `WebcamsListScreen.tsx` to use `SelectBox` for Road selection.
- [x] Extract unique provinces from `MOCK_DATA`.
- [x] Add `selectedProvince` state.
- [x] Add second `SelectBox` for "Filtrar provincias".
- [x] Add labels "Filtrar carreteras" and "Filtrar provincias".
- [x] Update `filteredWebcams` logic to include province filter.
- [ ] Update header text to reflect both filters (optional, or just "Filtered Webcams").
</tasks>

</artifact>
