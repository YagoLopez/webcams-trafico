# Unit Tests Plan

Identify units (functions, components, hooks) altered or created during the implementation tasks.

## Unit Tests Checklist

- [x] `ArrayPaginator.paginate` correctly handles different page sizes and offsets.
- [x] `ArrayPaginator.paginate` accurately reports `hasNextPage`.
- [x] `useInfiniteFilteredCams` hook correctly formats the data for the component.
- [x] `CamListScreen` correctly renders the number of columns based on screen width.
