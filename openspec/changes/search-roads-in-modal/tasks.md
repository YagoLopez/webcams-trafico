<artifact>
<id>tasks</id>
<change>search-roads-in-modal</change>
<type>tasks</type>

<tasks>
- [x] Add `roadSearchQuery` state to `WebcamsListScreen`.
- [x] Add `TextInput` and search icon styling inside the `Modal`.
- [x] Implement filtered list data source for the modal (based on `roadSearchQuery`).
- [x] Add logic to clear search when closing/opening modal.
- [ ] Verify search functionality filters results correctly.
</tasks>

</artifact>
